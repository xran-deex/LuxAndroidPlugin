apply plugin: 'groovy'
apply plugin: 'maven'

repositories {
    flatDir {
        dirs "."
    }
}

dependencies {
  compile gradleApi()
  compile localGroovy()
  compile "com.github.luxlang:LuxGradlePlugin:0.1-SNAPSHOT"
}

group = 'com.github.luxlang'
version = '0.1-SNAPSHOT'
